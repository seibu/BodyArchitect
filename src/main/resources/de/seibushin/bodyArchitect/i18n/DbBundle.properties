stmt.create.0=create table if not exists FOOD(id integer primary key autoincrement, name VARCHAR NOT NULL, weight DOUBLE NOT NULL, portion DOUBLE NOT NULL, kcal DOUBLE, protein DOUBLE, fat DOUBLE, carbs DOUBLE, sugar DOUBLE)
stmt.create.1=create table if not exists MEAL(id integer primary key autoincrement, type varchar not null)
stmt.create.2=create table if not exists MEAL_FOOD(id integer primary key autoincrement, f_id integer REFERENCES FOOD(id), m_id integer REFERENCES MEAL(id), weight DOUBLE NOT NULL)
stmt.create.3=create table if not exists DAYS(id integer primary key autoincrement, "date" date not null)
stmt.create.4=create table if not exists DAY_MEAL(id integer primary key autoincrement, d_id integer REFERENCES DAYS(id), m_id integer REFERENCES MEAL(id))

stmt.insert.0=insert into FOOD values(null, 'Banane', 100, 100, 93, 1.2, 0.2, 20, 17.3);
stmt.insert.1=insert into FOOD values(null, 'Nudeln', 100, 125, 350, 12, 1.2, 71, 3.2);